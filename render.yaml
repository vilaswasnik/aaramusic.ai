services:
  - type: web
    name: aaramusic
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm ci && npm run build:render
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
