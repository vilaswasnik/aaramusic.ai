services:
  - type: web
    name: aaramusic
    runtime: node
    buildCommand: npm ci && npx expo export --platform web
    startCommand: node server/proxy.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
